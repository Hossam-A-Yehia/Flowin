# Docker Development Environment Variables
# This file is used by docker-compose.yml

# Database
POSTGRES_DB=flowin
POSTGRES_USER=flowin
POSTGRES_PASSWORD=flowin_dev_password

# Application
NODE_ENV=development
JWT_SECRET=docker-dev-jwt-secret-key
API_URL=http://api:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Redis
REDIS_URL=redis://redis:6379

# Database URL for containers
DATABASE_URL=postgresql://flowin:flowin_dev_password@postgres:5432/flowin
