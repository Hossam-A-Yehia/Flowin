FROM node:20-alpine

WORKDIR /app

# Install Prisma CLI globally
RUN npm install -g prisma

# Copy prisma schema
COPY prisma/ ./prisma/

# Expose Prisma Studio port
EXPOSE 5555

# Start Prisma Studio
CMD ["prisma", "studio", "--port", "5555", "--hostname", "0.0.0.0"]
